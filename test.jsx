const { useState, useEffect, useMemo } = React;
const { Search, Filter } = Lucide;

function FFXIVMacroDatabase() {
  return (
    <div>
      <h2>FFXIV Macro Database Component Loaded!</h2>
    </div>
  );
}
